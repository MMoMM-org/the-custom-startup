# Specification: 019-observability-rollout-across-active-repos

## Status

| Field | Value |
|-------|-------|
| **Created** | 2026-09-08 |
| **Current Phase** | PLAN |
| **Decomposition tier** | Incremental |
| **Last Updated** | 2026-09-12 |

## Documents

| Document | Status | Notes |
|----------|--------|-------|
| requirements.md | completed | 26 acceptance criteria, 0 clarification markers, 1 open question carried (collection-period end date) |
| solution.md | completed | 4 components, 8 ADRs all confirmed, 26 acceptance criteria |
| plan/ | completed | Incremental tier: 4 phases, 20 tasks, 2 parallel, 76 spec references |

**Status values**: `pending` | `in_progress` | `completed` | `skipped`

**Decomposition tier**: `Direct` (no plan) | `Incremental` (phase plan). Set by the classifier at the decomposition step and confirmed by the user; leave the placeholder until then. Read back by `spec.py --read`, which treats anything it does not recognise as absent.

## Decisions Log

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-09-08 | Spec opened | spec-018 shipped a working instrument and left it pointed at a repository where little work happens. Its own Won't-Have deferred distribution with the condition "revisit once the record has answered a real question" — but that condition can never be met from here, because the premise underneath it ("the evidence is needed *here*") is false. The maintainer works actively in four other repositories. This is a corrected premise, not an override of the decision |
| 2026-09-08 | Branched from the spec-018 branch, not `main` | spec-018 is unmerged and `main` ends at spec-017. `scripts/observability/report.py` — which this spec extends — does not exist on `main` at all, so a spec written from there could not reference its own starting point |
| 2026-09-08 | **No real repository names or paths in any COMMITTED artifact** — spec documents, commit messages, code. Targets are `repo1`–`repo4` there. **The report's own output is the opposite case: it names repositories, deliberately.** | Maintainer instruction, refined 2026-09-08 after an over-broad first reading by the author of this row. The two contexts have nothing in common. A spec document is committed to a PUBLIC repository (`gh repo view` → `visibility: PUBLIC`) and travels wherever the repository travels; the names of the repositories someone works in are exactly the context this spec exists to keep separate, so publishing them in the document arguing for that separation would be self-defeating. A report is generated locally, on the maintainer's own machine, from records that never leave it. There, the repository name is not an exposure but **the payload**: "does skill A fire in both repositories where it is supposed to?" is the actionable question, and it is unanswerable from a pooled total. Anonymising the report would delete its usefulness to protect against a disclosure that is not happening |
| 2026-09-08 | **Per-repository detail is the reporting default; a union figure is added only where it answers something a per-repository view cannot** | Follows from the row above, and independently from the research: merging is *misleading* for recording status and for the hook `installed` flag, because one live repository would make the whole report read fresh and hide stale logs — inverting spec-018's SDD-AC-15 honesty rule. Firing coverage is the one analysis that genuinely gains from merging, and notably the one that needs no repository identity at all: it unions numerators against a single shipped-inventory denominator. So the split is not a compromise between usefulness and honesty — the same line satisfies both |
| 2026-09-08 | ~~**Registration follows the satori pattern: an absolute path resolved at setup time.**~~ **SUPERSEDED TWICE the same day** — by the row below on *which file*, and by ADR-2 in `solution.md` on *the mechanism*. What survives of this row is only its negative half: the plugin `hooks.json` route was rejected, and the reasons below still hold. The absolute path did **not** survive: ADR-2 replaced it with a `$HOME`-relative bundle, because an absolute path cannot express the container and host shapes from one command string, and has no upgrade path. A first attempt to amend this row scoped the amendment to "which file" and explicitly claimed the mechanism still stood — which left the contradiction in place and a validator caught it. | Chosen over a plugin `hooks.json` registration. The plugin route resolves paths more cleanly via `${CLAUDE_PLUGIN_ROOT}`, but registers the hooks in *every* repository with the plugin enabled, each paying ~3-5 ms per matching event whether or not recording is on — a floor set by the macOS per-exec code-signature check, with script size and early-exit position both measured and refuted as causes. spec-018's Won't-Have list already rejected a permanently installed timing layer on exactly that reasoning. It is also unproven: no manifest in this repository registers `InstructionsLoaded` or `SubagentStart`, and the first of those is the event that carries the instruction data. The satori pattern touches only the repositories named, needs no plugin publish, and its merge half is proven in-repo |
| 2026-09-08 | **Setup writes into each target's `.claude/settings.local.json`, never `.claude/settings.json`** | Found while checking a residual risk rather than by design intent, and it inverted the obvious choice. In three of the four targets `.claude/settings.json` is **tracked in git**, and all four have remotes. Writing the registration there would put it in the maintainer's diffs, let it be committed by accident, and — once pushed — start recording for anyone who clones the repository and opens a session, with no reason to look for it. That is precisely the breach spec-018's PRD Feature 3 names, arriving one clone away rather than through a plugin update. `settings.local.json` is untracked in all four. It is also proven to carry hooks: one of the four already has a `hooks` block there, which is empirical evidence that the harness honours them at that layer rather than a claim from documentation. The satori precedent supplies the merge mechanism, not the choice of file — that choice is ours, and the evidence settles it |
| 2026-09-08 | **Success is defined as a decision becoming possible, not as records existing** | Maintainer choice. The criterion is that after a stated collection period the report can say, for every shipped skill and agent, in which target repositories it fired and in which it did not — reliably enough to decide keep, drop or rework. A criterion phrased as "recording works" would be met on day one and would leave the feature in exactly the state spec-018 left it: built, correct, and answering nothing. **Corrected 2026-09-08:** this row originally called that "#147 becomes decidable". An alignment audit checked the issue and it asks something narrower — which user auto-memory entries belong where subagents can see them, decided on whether an entry must reach every agent on every run rather than on usage counts. The firing coverage this criterion describes serves **#153** (this work's own issue, which promises #147 its denominator) and **#155**. Only the instruction-load half reaches #147. A success criterion that named an issue its work could not close was the most consequential error in this spec |
| 2026-09-08 | **The setup command is general and reversible, not a four-repository script** | Maintainer choice. It accepts any target, detects the host or container shape itself, merges without damaging foreign entries, reports honestly when it finds entries it does not own, and can uninstall. The incremental cost over a hard-coded script is small because the merge logic is required either way — and a setup path with no uninstall path is a trap, leaving entries to be pulled out of several files by hand later |
| 2026-09-08 | **Decomposition tier: Incremental.** Classifier recommended Incremental; maintainer confirmed | Rule 1 fired twice over: `component_count` 3 (bundle installer, registration editor, locations config are new surface; the report aggregation modifies existing code and so does not increment) and `feature_count` 4. `ac_count` 26, `change_type` feature, `parallel_markers` **false** — the two "concurrent" mentions in the SDD are a requirement that concurrent setup runs must serialize, which is a safety property of the lock rather than a parallel work stream, so a naive grep would have set this signal wrongly. Not a borderline case: breadth vetoes Direct regardless of change type, and both breadth signals cleared the threshold independently |
| 2026-09-08 | **The observability bundle marker uses the `h<N>` form (`h1`), not semver** | Resolved during implementation: T1.1 contradicted itself — step 2 asked for "a single semver line", step 4 for a format that "matches the existing git-helpers marker exactly". The two existing markers in this repository are `h6` (`tcs-git-helpers-version`) and `h1` (`tcs-helper-rule-enforcer-version`), so the two clauses were mutually exclusive. `h<N>` wins: it is what ADR-3 means by "the spec-012 pattern", and `plugins/tcs-git-helpers/README.md:34` records the reason the bundle version is deliberately decoupled from the plugin semver — a user staying on an older plugin version can still receive hook improvements. Semver would have created a second convention for the one CI gate to special-case. Maintainer confirmed; step 2 of T1.1 corrected in `plan/phase-1.md` |
| 2026-09-08 | **T1.2 fault injection goes through an extracted `_write_bundle_marker()`, overridden in bats** | The plan told developers to test the atomic marker write by "replacing the marker write with a failing stub", and justified naming that seam by noting the same property stated abstractly "would have been tested three different ways by three developers" — but it named only *what* to inject, never *how*, so it reproduced the divergence it claimed to prevent. A TDD gate blocked T1.2 on it. Three readings were live: a function override, a `PATH` shim on `mv`, and a `trap` interrupt — each testing a different property. Function extraction wins: deterministic, isolates the seam (a `mv` shim would hit every other move in the installer), and matches the repo`s existing `_read_*` function-extraction style. `trap` tests interruption, which is a real concern but belongs to ADR-4 and phase 2`s settings write, not to this marker. Also pinned in the same edit: idempotence asserts `sha256` tree sums (not mtimes, not inodes), and the adapter case must execute a copied adapter and read back its record |
| 2026-09-08 | **`README.md` ships inside the installed bundle but is NOT a CI-gated bundle source** | Raised by the T1.2 implementer as a judgement call it did not want to make silently. Installing it is worth it: `$HOME/.claude/observability/` is a directory that records the user`s own activity, and someone who finds six unexplained shell scripts there deserves a file telling them what is running. But gating it would mean a documentation typo bumps the version marker, at which point every installed bundle in every target reports `DRIFT:` and prompts an upgrade that ships byte-identical scripts. Drift that fires on prose trains people to ignore drift — the precise failure ADR-3`s machinery exists to avoid. So T1.4`s gate covers the six executable scripts only. Cost accepted: the gate encodes a source/installed distinction rather than treating the copy set as the gate set |
| 2026-09-08 | **T1.3 mirrors the drift comparator`s contract rather than sourcing its code** | The plan said "reuse the existing comparator rather than reimplement", and that instruction cannot be followed. `drift_check_hook_bundle` only looks generic: its third argument varies the marker`s *filename*, but its body hardcodes `${repo_path}/.githooks/${version_filename}` — and this bundle lives at `$HOME/.claude/observability/`, a path with no `.githooks` segment, so no argument combination reaches it. Independently, `drift_check.sh` lives in `tcs-git-helpers` and this code in `tcs-helper`; a repository-wide search found **zero** cross-plugin `source` lines, all six plugins carry their own `.claude-plugin` manifest, and sourcing across that boundary would create the first cross-plugin runtime dependency here and break `tcs-helper` when installed without `tcs-git-helpers`. Ruling: reproduce the `OK`/`MISSING`/`DRIFT:<installed>` contract exactly in this skill`s own `lib/`, commented with a pointer to the original so the duplication reads as deliberate. Roughly 15 duplicated lines, bought against a dependency the distribution model does not support |
| 2026-09-08 | **A malformed installed marker reports `DRIFT:<raw content>`, not an error** | T1.1`s reader rejects a malformed marker (exit 1, stderr "malformed"); the comparator T1.3 mirrors accepts anything via `head -n 1 \| tr -d "[:space:]"` and would report `DRIFT:hello`. Two components of one skill, two policies, and nothing in the plan said which — an implementer would have picked silently. Faithful mirroring wins: garbage is not the expected version, so `DRIFT:` is truthful and the remedy (reinstall) is the same as for a stale marker. T1.1`s strictness existed so a broken marker could never compare *equal* to the expected version, and `DRIFT:` never compares equal — the hazard it guarded does not arise here. Keeping three states also honours the earlier ruling to reproduce `OK`/`MISSING`/`DRIFT:` exactly, and spares T4.1`s status verb a fourth case. Asserted explicitly in the tests so it reads as a decision |
| 2026-09-08 | **T1.4`s gate matches `*.sh` under the sources directory rather than consuming a filename list** | A TDD gate blocked T1.4 partly on an unresolved question: should a `tcs-git-helpers` CI script source `_BUNDLE_INSTALL_SCRIPTS` from a `tcs-helper` skill lib, or duplicate the six names? Checking how the gate actually runs dissolved the question. It is invoked by a workflow against a full monorepo checkout, so the plugin-independence rule that forced T1.3`s correction does not bind — but more usefully, the existing gate never used a list at all: it matches by **directory prefix**. Extending that shape means gated = any `*.sh` under `plugins/tcs-helper/scripts/observability/`, which excludes `README.md` by extension with no exclusion list, and gates a newly added adapter automatically. Consequence worth recording honestly: this makes T1.2`s `_BUNDLE_INSTALL_SCRIPTS` split unnecessary for its stated purpose — it was introduced on a code review`s argument that T1.4 would otherwise duplicate the six filenames. The split is harmless and documents intent, but it was not needed |
| 2026-09-08 | **This repository migrates to the standard mechanism; its hand-made registration is not accommodated permanently** | Measured while building phase 2`s fixture matrix, and it contradicted the design in two places at once. This repository`s live registration is in `.claude/settings.json` (not `settings.local.json`, which holds no hooks here) and its commands point at `$CLAUDE_PROJECT_DIR/plugins/tcs-helper/scripts/observability/` (not the `$HOME/.claude/observability/` namespace ADR-5 uses to prove ownership). So detection per ADR-1 would call this repository **clean** and register a second time beside a live registration — double recording in the one repository the collection period depends on — while detection that also read `settings.json` would call our own scripts foreign. The PRD edge case "recognised as already configured rather than duplicated" was therefore not satisfiable as specified. Maintainer ruling: migrate. The hand-made entries come out, setup installs the standard registration, and the migration runs in phase 4 once the setup command exists. Detection must still classify the legacy shape distinctly in phase 2 — never `clean`, never a bare `conflict` — because setup meets that state during the migration, and mishandling it is precisely how the migration would create the duplicate it exists to avoid |
| 2026-09-08 | **Detection reports state on stdout and severity in the exit code; `legacy` and `ours-old` are warn, not abort** | A TDD gate blocked T2.2 on a real arithmetic problem: the plan names eight target states and the severity convention has four exit codes, with no stated mapping — and T4.1 branches on it, so a guess here would have become phase 4`s contract. The precedent dissolves it: `detect_conflicts.sh` already emits a labelled line per finding and keeps only the highest severity as the exit code, so state and severity were never one channel. Fixed map: CLEAN and OURS-CURRENT exit 0; OURS-OLD and LEGACY exit 4 (warn); CONFLICT exits 3; not-a-repository, unparseable, valid-JSON-of-the-wrong-shape and write-path-not-ignored all exit 2 (abort). `legacy` warns rather than aborts on purpose — the approved migration of this repository runs through setup, and aborting would force a manual two-step on the one repository that most needs a clean migration. Also closed: every one of the 13 fixtures now has a stated classification, valid-JSON-wrong-shape aborts with a diagnosis rather than a traceback, and the read-only assertion must cover all three files detection reads |
| 2026-09-09 | **A read path counts as covered only where skipping it would change the reported answer** | T2.2's spec-compliance review returned FAIL on the pin requiring the write-protected case to exercise all three files detection reads. The test named all three but built its target from `foreign-plus-ours-current`, the fixture matrix's most obvious choice and one of the twelve that carry no `.claude/settings.json` at all — so `load()` returned at `os.path.isfile()` and the shared path was never opened. That is the phase-1 defect the pin names by name, in a new form; the earlier switch from an `EUID` check to a write canary had not touched it. The fix went further than the pin's letter for a reason found while making it: **presence alone cannot prove a read happened.** A failed `open()` returns `(None, err)`, `is_legacy(None)` is `False`, and the answer falls through to the same label a successful negative read produces — so an assertion on a merely-present file cannot fail. Hence two cases: one target carrying all three files with foreign non-legacy shared content (classification stays `OURS-CURRENT`, so the marker must still be read), and one carrying the legacy shape, whose answer changes to `LEGACY` **only if the file is truly opened and parsed**. Proven by mutation rather than argued: with `load(shared_path)` stubbed to `(None, None)`, the original test still reports `ok` and the new one reports `not ok`. `detect.sh` was not touched — the defect was in the test, and it stayed there. The reviewer checked the adjacent question unprompted and found the local-settings and marker reads already sensitive to a silent skip, so the shared path was the only one with this hole |
| 2026-09-09 | **The `exactly one state line` loop stays at 11 of 13 fixtures; carried, not folded into T2.2** | The loop omits `foreign-plus-ours-current` and `foreign-plus-ours-older`, the two needing a `_BUNDLE_INSTALL_TARGET_DIR` argument it does not pass. Reviewer's recommendation, accepted: the pin requires one labelled line per target as a property of `detect.sh`, and that property does hold for both — verified through their individual tests' `_assert_not_contains` pairs rather than the centralised count. Lower assurance, not absent assurance, and not a named gate. Reopening a passed task for a three-line coverage improvement costs more in review churn than the assurance is worth |
| 2026-09-09 | **A structurally malformed foreign entry under one of our own event names aborts the registration with a diagnosis, rather than being skipped so ours can still be added** | Produced unasked by T2.3's fix round, and it changes the task's contract, so it is recorded rather than left in a commit body. Before the fix such an entry crashed `entry_is_ours` with an uncaught AttributeError -- reproduced three ways (`hooks` a string, a list carrying a non-object element, a dict), each exiting 1 with a traceback. The file was left byte-identical every time, so this was a diagnosis-quality defect and never data loss, and it is recorded that way rather than as a near-miss. Two behaviours were available: abort with a diagnosis, or treat the unreadable entry as not-ours and register anyway. Abort was chosen and confirmed by the maintainer. It matches the ruling already recorded for detection -- valid JSON of the wrong shape aborts with a diagnosis rather than a traceback -- and follows ADR-1: refusing to guess at a structure we cannot interpret is the safe move on a file no version control can restore. Spec compliance ruled it consistent with the clause "a foreign hooks block survives, including foreign entries under the same event name": nothing is written, so the foreign block does survive; what does not happen is our registration. The seven task clauses only ever described well-formed foreign entries, and that test is unmodified and green |
| 2026-09-09 | **`entry_is_ours` keeps its tolerance for malformed entries even though the CLI cannot reach it** | The same fix round produced two fixes for one defect: a tolerant `entry_is_ours` and an upstream validation loop that raises before it is ever consulted. Mutation exposed the redundancy -- reverting the tolerance to its original one-liner left all 15 tests green, which is how dead defensive code announces itself. Deleting it was the obvious cleanup and was rejected: T2.4's removal path calls `entry_is_ours` to identify our entries where that validation loop does not run first, so the tolerance is load-bearing for a named next task rather than speculative. It stays, covered by six direct unit tests that import the function instead of driving the CLI, with a comment recording why they bypass it so a later reader does not delete apparently-unreachable code. The same mutation now kills four of them |
| 2026-09-09 | **An older bundle version is recognised by a differing command, not by a version field; and the round-trip clause is really a demand that removal prune what install created** | Two readings T2.4 could not be implemented without, settled before dispatch and confirmed by the TDD gate. The `h<N>` marker `lib/bundle_version.sh` reads tracks the BUNDLE in `$HOME`, not a per-target registration, so the settings file carries no version anywhere. An entry from an older version is therefore one `is_ours` accepts by namespace whose command or matcher differs from what `command_for()` produces today -- precisely the case ADR-5 exists for, and it means `registration.py` needs no dependency on the marker at all. "Already configured" becomes every expected entry present AND identical; "update" becomes at least one of ours present but differing. The second reading was measured rather than argued: a 4-space-indented `{"model": "opus"}` comes back 2-space indented after install, and install adds the top-level keys `env` and `hooks` where there were none. So byte-identity after install-then-remove is testable only from a canonically formatted starting file, and the substantive half of the clause is that removal must tear down the containers install created. The round-trip test now passes only if that pruning works, which makes it a real assertion of the rule rather than an incidental one |
| 2026-09-09 | **Correcting only the env switch's value counts as an update, and the message names the registration rather than the hooks** | Found by code review and reproduced: take a fully registered document, hand-edit `CLAUDE_OBSERVABILITY_ENABLED` from `1` to `0`, re-run setup, and it reported "updated observability hooks" though no hook entry was touched. Two things were wrong in different directions. The status was right and stays `update`: something of ours was stale and got corrected, which is exactly what SDD-AC-8 asks a caller to be able to tell apart from a fresh install. The message was wrong, and now reads "updated observability registration" -- accurate in both cases, since the registration is the env switch and the hooks together, not the hooks alone. No acceptance criterion pins the wording; AC-8 requires only that an update be distinguishable from an install, which the reporting test asserts. The path was untested in either direction and now has its own case |
| 2026-09-09 | **Carried to T4.1: the removal path's own messages are asserted by nothing** | Raised by T2.4's spec-compliance review and deliberately not fixed there. PRD F2 promises the system reports which entries were removed, and says so when there was nothing to remove; both messages exist in the code and neither is asserted by any test. This is not a T2.4 gap by the task's own wording -- the reporting clauses it enumerates are the install, already-configured and update ones, and those are now covered. It is recorded here because T4.1 tests the four journeys through the real entry point, and that is where these two messages get their only natural coverage. If T4.1's test plan does not name them, this row is the reason it should |
| 2026-09-09 | **The temporary file is created in the target's own directory, not in `$TMPDIR` -- the precedents this task names are unsafe for this case** | T2.5 orders two of this repository's own scripts read as the model for atomic replacement, and both do `mktemp` then `mv`. `mktemp` with no argument creates in `$TMPDIR`. Measured rather than assumed: the repository sits on st_dev 16777245 and `$TMPDIR` on 16777234, so `mv` there is copy-then-unlink, not a rename. Ported literally into Python this is worse than merely non-atomic -- `os.rename()` raises EXDEV and the obvious rescue, `shutil.move()`, copies INTO the destination path, which opens the real file for writing. That is the exact truncation window this task exists to close, reintroduced by an implementer who looks like they are following house style. The rule is therefore `tempfile.mkstemp(dir=os.path.dirname(target))` plus `os.replace()`, following the precedents' intent and not their shape, said so in a comment so nobody restores it. A dedicated test asserts the temp directory's `st_dev` matches the target's, and it needed its own fixture: pytest's `tmp_path` is itself under `$TMPDIR`, so the assertion passes while the bug is present |
| 2026-09-09 | **A half-written lock is not an abandoned one; the race was fixed here rather than carried, because SDD-AC-11 is ours** | `_try_acquire_once` creates the lock with `O_CREAT\|O_EXCL` and writes `<pid>:<epoch>` on the next statement, so the file is briefly empty. A contender reading it in that window got no owner, judged it unreadable and therefore stale, and unlinked the lock out from under its live owner -- after which both runs proceeded into one settings file. Code review found it and classed it non-blocking on the grounds that the bash precedent has the identical race and the port was faithful. Overruled: SDD-AC-11 is one of this task's two success criteria and says two concurrent runs serialize, so fidelity to a flawed precedent is no defence when the criterion is ours. Unreadable content is now stale only once the file has sat past `LOCK_GRACE` (2s, sized between a microsecond write window and the 300s TTL); dead PID and over-TTL keep immediate staleness. `lock.sh` carries the same bug and was deliberately not touched -- different plugin, different suite. What the grace does not close is recorded beside the constant: an owner stalled past 2s still loses its lock, and the airtight version writes the lock line to a temp name and renames it in, the technique this same module already uses for the settings file. Not built, because reaching the residual window needs a multi-second stall writing twenty bytes -- the same risk shape the 300s TTL already accepts when it reclaims from a live but frozen owner |
| 2026-09-09 | **Setup does not refuse a target whose sidecar files are un-ignored; that enforcement lives in detection and in the command, not in the editor** | A first draft of T2.5 added a runtime gate to `main()` that ran `git check-ignore` over all four written paths and refused the target if any was reachable by version control. It was rolled back before the commit landed. The plan says the `.bak` ignore status is T2.5's *assertion* -- a test -- and enforcement is SDD-AC-6, which T2.2 carries for the settings path and T4.4 step 3 for every path this feature writes. The gate also contradicted a sibling component: the fixture named `ignored-file-but-not-backup` is classified CLEAN by detection, so setup would have refused a target the same command calls clean. And it fired above the `--remove` branch, which would have made a target with un-ignored sidecars impossible to uninstall -- found only after the code was already gone. `written_paths()` survives as the honest declaration of the four paths this module can create, with no production caller: the Safety test reads it instead of rebuilding the list, so a path added to the writer without being added there is a bug the test catches |
| 2026-09-10 | **The fixture matrix is exercised by detection only; running setup against it shows the migration promise is unkept** | Phase 2's acceptance criteria say setup and removal are correct across every fixture in the matrix. Nothing ran `registration.py` against them -- `build.sh`'s output is consumed by the fixture-property tests and by `observability-detect.bats`, while the editor's own tests use hand-built inline documents. Closed by a new bats file that runs setup and removal over all 13 fixtures and pins today's behaviour. The case that matters: on `already-configured-observability`, setup reports "registered" and leaves SIX hooks firing where three should, because the legacy registration lives in `.claude/settings.json` under `$CLAUDE_PROJECT_DIR/plugins/tcs-helper/scripts/observability/` while the editor edits `settings.local.json` and proves ownership by the `$HOME/.claude/observability/` namespace. Not an editor defect -- it cannot see the other file by design -- but detection already prints "setup will migrate this", a promise on setup's behalf that only T4.1 can keep. Four further behaviours were undocumented until pinned: removal on a previously absent target leaves `{}` rather than restoring absence; a round trip restores the same data but not the same bytes wherever the pre-setup file was not already canonical; the editor is entirely git-blind, so `write-path-not-ignored` and `not-a-repository` behave exactly like `absent`; and setup validates the document's shape while removal never inspects it |
| 2026-09-10 | **`--settings` had no default by declaration and nothing held it there** | T2.6 asks that the override be asserted, not assumed. It was `required=True` with help text promising it "never defaults to a real one", and every one of the 51 tests passed it explicitly -- so giving it a default of `~/.claude/settings.local.json` left the whole suite green. A promise that survives its own removal is not tested. Closed by a test that invokes the editor with no `--settings`, points HOME at a temp directory, and asserts not only a non-zero exit but that nothing was created anywhere beneath it: the exit code alone would not catch a future default that exits 0 and quietly edits the operator's own file. Proven by mutation -- with the default restored, 51 pass and only the new test fails |
| 2026-09-10 | **Four perf tests in `tcs-git-helpers` fail on this machine at the default budget, and it is not load** | Recorded so the number is not mistaken for a regression. `bats plugins/*/tests/bats` is four suites, not the one this phase kept quoting: 1068 ok, 4 not ok, and every failure is a p99 budget. First read as contention, which was wrong -- on an idle machine `githooks_commit_msg` measures p50 171ms against a p99 budget of 150ms, so the median alone exceeds it. The suite already ships `TCS_PERF_SLACK` for exactly this, the tests belong to a different plugin than this spec, and the maintainer has deferred them until the rest is through. Also worth knowing for whoever runs the full glob next: it takes over half an hour, a run piped through `tail` reports `tail`'s exit status rather than bats', and a long run started from a tool call can be reaped before it writes anything -- redirect to a named file and count `^not ok` lines instead |
| 2026-09-10 | **`foreign-plus-ours-current` and `-older` do not carry the foreign entry their names promise** | Found while pinning the matrix. `build.sh:212-218` composes those two fixtures by concatenating a foreign hooks block and our own with a comma between them, which produces two top-level `hooks` keys. That is syntactically valid JSON and resolves last-wins, so after any parse only our entries remain and the foreign entry is invisible -- to `registration.py` and to anything else that loads the file. Their fixture-property test passes because it asserts against the raw text. Contained: only these two of the thirteen, consumed by `observability-detect.bats` and the property tests. Being fixed next, with the effect on detection's classifications measured rather than assumed |
| 2026-09-10 | **A golden fixture that reproduces byte-identically can still pin almost nothing — coverage breadth is the real bar, and the plan did not ask for it** | T3.0's Validate step is "the fixture reproduces byte-identically on a second run". That is necessary and badly insufficient, demonstrated rather than argued: a probe capture built with reasonable care reproduced byte-identically across two runs in two temp directories (2228 bytes) and rendered three sections empty with `Coverage: 0/0`, because its skills sat two levels under `skills/` and `walk_skill_agent_inventory` classified them all unreachable instead of counting them. T3.1, T3.3 and T3.4 could have rewritten an entire report section against a fixture like that and T3.5's diff would have stayed green. The accepted fixture therefore had to make every section non-empty and every sub-case distinct -- all four instruction layer labels, both byte-accounting edge counters, a non-`0/0` coverage fraction, unused entries, an unrecognised record name, an unreachable file, all three hook scope cases, and a non-zero unparseable count. Verified against the committed golden, not the implementer's summary of it |
| 2026-09-10 | **A fixture tree inside this repository's worktree inherits this repository's `.gitignore`, so the "not a git repository" branch never runs** | Found while capturing T3.0. The fixture must exercise `walk_instruction_inventory`'s unfiltered branch (`git_filtered=False`, "git unavailable or repo_root is not a git repository"). With the synthetic tree committed under `tests/fixtures/`, it is still inside this repo's worktree, so `git check-ignore` answered normally instead of failing closed: the root `.gitignore`'s bare `.claude/` rule silently swallowed the fixture's own `.claude/rules/*.md` two levels down, corrupting the captured inventory count without touching `report.py`. Nothing errored -- the capture succeeded and looked plausible, which is the whole danger. Fixed by building the tree under `tempfile.TemporaryDirectory()` at run time, outside any worktree, so the branch renders deterministically; the committed builder script is then the input specification. Sibling of the known `core.excludesFile` trap: that one leaks config, this one leaks containment |
| 2026-09-10 | **The byte-diff assertion moved from T3.5 to T3.0 by approved deviation** | Spec compliance ruled the suite-wired test scope creep and was right on the plan text: T3.0 step 2 says "Test: none yet" and T3.5 step 1 owns the fixture comparison. Overruled deliberately, by the maintainer rather than by the implementer. T3.0's whole purpose is to protect T3.1, T3.3 and T3.4 from silently breaking `--events`; an assertion landing only at T3.5 lands after all three have changed the reader, leaving its implementer accumulated drift across three commits with nothing to say which change caused what. Accepted only after mutation proved the test has teeth -- a renamed section label, a reversed `never_loaded` sort and an off-by-one in the unreachable count each failed it. T3.5 keeps its other two assertions and inherits an explicit instruction not to regenerate the golden to make it pass |
| 2026-09-10 | **`repo` arrives as a parallel function, not a re-keying -- and the obvious alternative is the defect wearing a new field** | T3.1 reads as "key instruction statistics on `(repo, path)`", which invites re-keying `instruction_stats` itself. That breaks roughly twenty existing tests that index its result by bare path string, and both `never_loaded()` and `build_load_report()` consume that exact `dict[str, InstructionFileStats]` shape -- so it also contradicts the task's own step 4, "existing single-record tests unchanged". The second obvious move is worse because it looks like it works: hanging a `repo` field on `InstructionFileStats` while keeping the dict keyed by path cannot represent two repositories' counts for one filename, since one object holds one `repo` value -- that IS the merge defect, renamed. Built instead as `instruction_stats_by_repo(records) -> dict[str or None, dict[str, InstructionFileStats]]`, outer key the repo and inner dict EXACTLY today's shape, so T3.3 can hand one straight to `never_loaded()` with no adaptation -- verified, not assumed. `instruction_stats` is untouched |
| 2026-09-10 | **The unknown bucket is keyed on Python `None`, because `repo` is an environment-controlled directory basename** | Step 2 asks that a record with an empty `repo` land in an "unknown" bucket rather than joining another repository's. A string sentinel would have been the natural choice, and there is precedent at `hook_duration_stats:1221` -- but that is safe only because its values come from Claude Code's fixed event vocabulary. `repo` comes from `_observability_repo_field` (`logwrite.sh:339-348`) and is the git toplevel basename, falling back to `$PWD`'s: an unconstrained name a user controls, so a clone called `unknown` is not far-fetched. A JSON-decoded string can never equal `None`, so `None` is the only structurally collision-proof key. Demonstrated rather than argued: a record whose repo is literally `unknown` stays separate from the bucket for records that have no repository at all. Reading the writer also settled a question the plan left open -- `repo` is a frozen, always-present field (`logwrite.sh:660,695`), but its VALUE can legitimately be empty (`:346,656`), and a truncated record can lack the key entirely; both reach the same bucket, the same unification `InstructionFileStats.record()` already applies to `reason` |
| 2026-09-10 | **The defect was measured before implementation, and the sum-invariant test does not defend the criterion it sits next to** | "The failing test is written first" went unheld six times in phase 2, so T3.1's RED was established as a number before anyone was dispatched: against the unmodified reader, two records from `repo-alpha` and `repo-beta` for the same `CLAUDE.md` returned ONE entry with `load_count=2`, and a record with an empty repo silently joined the named one. The gate also named what a fake RED looks like here -- a test calling a function that does not exist yet fails with `AttributeError`, which would fail identically for a typo, an empty stub or a correct implementation, proving nothing -- so the implementer was required to produce a value-mismatch instead. Three independent mutations were killed afterwards: a string sentinel, ignoring `repo` entirely, and splitting a missing key from an empty string. One test is honestly weak: the per-repo sums matching the per-file totals holds for ANY total-preserving grouping, including one that ignores `repo` completely, so it pins that records are neither dropped nor double-counted during regrouping and contributes nothing to SDD-AC-19. Kept, credited for what it actually guards |
| 2026-09-10 | **Accepted duplication, with the condition that reopens it** | `instruction_stats` and `instruction_stats_by_repo` repeat the same four-line kind/path filter. Left duplicated deliberately: unifying the accumulation as well is not possible without adding a merge method to `InstructionFileStats`, because `instruction_stats` must merge same-path entries across repositories while `by_repo` must not -- more machinery than the duplication costs, at two call sites. The narrower fix is named here so it need not be rederived: extract the filter alone as `_usable_instruction_path(rec) -> str or None` and have both loops call it. Do that when a third `*_by_repo` variant appears, which T3.3 may bring |
| 2026-09-10 | **CI runs Python 3.11 and this machine runs 3.14, and T3.2 sat exactly on the seam** | `.github/workflows/tests.yml:61,101` pins 3.11 and runs the whole suite on ubuntu and macos. `tomllib.TOMLDecodeError` carries structured `lineno`/`colno`/`msg` on 3.14 (cpython gh-126175) and **none of them on 3.11**, where the position survives only inside `str(e)`. The plan asks for "an error naming the line", so the obvious implementation reads `e.lineno` — which passes locally and raises `AttributeError` on both CI runners. Found by fetching 3.11 through `uv` and measuring, not by inspection; the gate reviewing this task verified on 3.14 and recommended `e.lineno` directly. Recorded in `docs/ai/memory/tools.md` because every remaining Python task in this spec runs under the same split |
| 2026-09-10 | **The remedy carried the same defect one layer down, and the message text turned out to be the version-stable path** | The natural fix — `getattr(e, "lineno", None)` with a message fallback — is also wrong, and hides the failure inside the cure. Measured against `'[[source]]\nlabel = '` (an error at end of document): `getattr` yields **2** on 3.14 and no attribute on 3.11, while the message yields nothing on both. So the same malformed config produced two different user-facing errors depending on the interpreter, and the implementer's 18 tests passed on both because none covered that case — it had verified the fallback branch *executes*, which is not the same as the two branches *agreeing*. The ruling inverts the intuition: the "native" attribute is the unstable path and the message is the stable one, measured byte-identical across 3.11.14 and 3.14.3 over four malformed shapes. `.lineno` is never read, not even as a preferred path, and `test_malformed_toml_at_end_of_document_has_no_lineno` pins the single input where the two designs disagree so nobody "simplifies" it back |
| 2026-09-10 | **`sources.py` imports nothing local, which is what makes the CON-3 test meaningful and T3.3 possible** | Step 1 says the reader must feed `_resolve_events_path` rather than duplicate it, which reads like an instruction to import it. Rejected: T3.3 needs `report.py` to import `sources.py`, so the reverse edge is a real module-level cycle. `sources.py` therefore parses the config into `repo_root` plus `homes` and lets the caller resolve — the plan's own Key Decision, that the config stores no derived path and "cannot drift from the resolver that consumes it", is the design and not merely rationale for the schema. Two consequences worth having: the CON-3 allowlist is exactly `sys.stdlib_module_names` with no local exception to special-case, and the T3.0 golden fixture stays green for free because nothing here touches `report.py`. The formula is duplicated once, deliberately, and a test calls both private functions to pin that they agree — the only mechanism left once importing is barred |
| 2026-09-10 | **The CON-3 check parses the AST rather than inspecting `sys.modules`, and mutation proved the difference is real** | A runtime check only sees whichever branch ran: a `try: import tomli / except ImportError: import tomllib` guard passes on any machine where `tomli` is absent, and an import inside a never-called function is invisible entirely — which is exactly how an optional dependency arrives. Three mutations were run against the finished task: restoring the `getattr`-first extraction (caught by the end-of-document test), a top-level `import yaml` (caught), and an `import yaml` hidden inside a function that is never called (**caught** — the case that justifies the design, and the one a `sys.modules` diff would have missed) |
| 2026-09-10 | **ADR-7's collapse, demonstrated on the same records rather than argued** | The phase's central claim is that merging recording status is dishonest. Measured: two sources, one whose newest record is from today and one from 101 days ago, both `enabled=1`. Merged, `recording_status` reports `stale=False` with the fresher timestamp -- the dead source is invisible, because `newest_ts` takes a max. Split, the second renders `STALE` naming its own `2026-06-01` entry. Both forms are now permanently committed side by side: a test pinning the merged behaviour (which passes, and documents the defect) directly above the split one, so the contrast is legible rather than something a future reader has to reconstruct |
| 2026-09-10 | **`--events` keeps the golden fixture green because the mode branch lives in `main()`, which the SDD had already settled** | The risk going in was that per-source rendering would move `--events` output and someone would meet a red golden test with `--write --force` one keystroke away. It cannot: the SDD says `--events` "continues to mean 'this one record', so every existing invocation keeps working", making it a mode selector that never consults the config. `build_load_report` gained two more optional `None`-default keywords, the same precedent every prior extension used, and never branches on how many sources exist. The golden path is not executed by the new branch at all. This converts a judgement call into a property CI checks on every push, on two interpreters: **if the golden goes red, the change is wrong, not the fixture** |
| 2026-09-10 | **The plan's own line references had gone stale, and a section had no stated way to find its own data** | T3.2's text cited `_resolve_events_path:1561-1573`; T3.1's 42 added lines had moved it to `:1603`, so the cited range pointed at the renderer. Worse, nothing said how a per-source section finds its entry in `instruction_stats_by_repo`. The outer key is the record's own `repo` field, which `logwrite.sh` freezes to the git toplevel's **basename**, while `Source.label` is free text a human chose -- and every test the plan specified passes whichever you pick, because none used a config where they differ. Ruling: look up by `repo_root.name`, render under `label`, and add the missing test. Mutation confirms it now bites: swapping the lookup to `label` fails three tests |
| 2026-09-10 | **Ruling (a) was chosen from a rendered preview and then implemented by nobody** | The maintainer picked per-home sub-lines over a single collapsed verdict, on the stated grounds that a source whose second home died months ago would otherwise look identical to a healthy one. T3.3 shipped the headline and no sub-lines -- the rejected option. `Source.verdict` and `HomeStatus.state` were computed, thoroughly tested, and read by nothing outside their own tests. It survived a gate, an implementer and a spec-compliance pass because every test asked whether those values are computed correctly and none asked whether anything consumes them. Caught by a code-quality reviewer asking what the NEXT task inherits. Now rendered, with a guard test that fails if `state` goes write-only again |
| 2026-09-10 | **The best decision in the fix was itself unprotected** | Each home's `newest_ts` is computed before the homes' streams concatenate, so a home cannot advertise a fresher sibling's timestamp -- the implementer did this unprompted and wrote down why. Nothing held it. Changing `newest_ts(home_records)` to `newest_ts(records)`, one token, left all 772 tests green while the output changed: a home that last recorded a day earlier began displaying its sibling's timestamp, which is the ADR-7 collapse one level down. The existing fixtures could not catch it because every multi-home case had at most one RECORDING home, and a missing home renders no timestamp to borrow. Closed by a test with two recording homes at different timestamps, asserting the older line does NOT contain the fresher value. Eighth instance this spec of a promise that lived only in prose |
| 2026-09-10 | **The report's `unreachable` detection was checked against ground truth for the first time, and was right** | Run against this repository while priming T3.4, `walk_skill_agent_inventory` reported a denominator of 83 entries (64 skills, 19 agents) and **15 unreachable `SKILL.md` files — every one of them in `plugins/tcs-team/`**, all sharing one shape: `skills/<category>/<name>/SKILL.md`, a category directory between `skills/` and the skill, where the harness only ever looks at `skills/<name>/SKILL.md`. Put to the maintainer rather than asserted, since "the harness cannot discover this" is a claim about Claude Code and not about our code: confirmed known, and the fix is to flatten them into `skills/`. That is the first time this section has been validated against a case whose answer was independently known — every prior test of it used a fixture built to produce the expected result. Also worth recording as the shape of finding this instrument exists to produce: 15 shipped skills that can never fire no matter how anyone works, invisible to usage data because they never appear in a record at all. Not spec-019's to fix |
| 2026-09-10 | **The union was correct, well tested, and wired to only the first source — and 162 tests said nothing** | T3.4's own fixture is carefully built to distinguish a union from an intersection: skill X fires only in source A, Y only in B, Z in both, W in neither. It kills that bug. What it could not see was `main()`, where the union is actually assembled, because it exercises the composition directly — and the test's own docstring said so: "exercises that composition directly rather than through the CLI, since the union renders in `main()`". Changing that call site to `[:1]`, so only the first source contributes, left the whole suite green. **This file already contained the fix, applied once before**: a spec-018 reviewer hit the same hazard and left a comment explaining that `main()` is the only real caller wiring `byte_stats=`/`recording=` in, so its test asserts on actual CLI stdout. The lesson was recorded in the right place, in prose, beside the code, and the next person did not apply it. Closed by a CLI test asserting the union's rendered content — not merely its header, which the same mutation survives, since the per-source detail still credits the source correctly while the union alone collapses |
| 2026-09-10 | **Nine instances now, and the shape has never varied** | Every one: a value is computed correctly, thoroughly tested, and nothing checks that the program consumes it. `entry_is_ours` tolerance, "only what THIS call emptied", index preservation on replace, `--settings` never defaulting, `detect.sh`'s migration promise, the `foreign-plus-ours-*` fixture names, ruling (a)'s per-home sub-lines, the per-home `newest_ts` not borrowing, and now the union's wiring. Six in phase 2, three in phase 3. In each case the tests existed and were pointed at computation rather than consumption, and in each case a single-token mutation exposed it in seconds. Reading the diff has never once found one of these; mutation has found all nine |
| 2026-09-10 | **A ruling and its own delegation brief disagreed, and the implementer said so rather than picking one** | Ruling (p) requires each source's detail to name entries that fired in another source but not this one. The brief paraphrased that as "source A's detail does not show Y" — which, read literally, forbids exactly what the ruling requires. The implementer resolved it by keeping A's *fired-here* set free of Y while naming Y under a separate "fired in another source but not here" line, and flagged the tension for reversal rather than quietly choosing. That is the right behaviour and the paraphrase was the defect. Recorded because the failure mode it avoided — an implementer silently resolving a contradiction in its instructions — would have been invisible in review |
| 2026-09-10 | **Phase-3 validation evidence, measured rather than inferred from a green suite** | T3.5 must confirm every spec-018 report test still passes *unmodified*. A green suite cannot show that: a quietly weakened assertion looks identical. Measured across the whole phase (`eb9b529..079e0fd`): the report test file went **136 to 163 tests with zero removals and zero renames**, +915/-2, where both deletions are a helper's hardcoded value replaced by a parameter with the same default, and one docstring line. `report.py` is +423/-9, every deletion a comment line or the single assignment that moved during the `main()` extraction. No deviation to record |
| 2026-09-10 | **Two conventions settled so phase 4 need not relitigate them** | First, `spec-019 T3.x` in comments: the ambiguity is real (two specs numbered their tasks identically in the same two files), but measured, `report.py` carries 64 bare references against 17 prefixed and 37 of the bare ones predate this phase entirely. The prefix therefore applies to **new** comments in these two files only; an existing bare reference is not a defect and reviewers should stop reporting it as one. Second, a compute/render split: `_render_per_source_firing_detail` calls `firing_coverage()` internally to build its own data, while `_render_firing_coverage_union` and the pre-existing `_render_firing_coverage` both take an already-computed `FiringCoverage` and only render. No behavioural risk, but it is drift from a pattern the rest of the module follows scrupulously — hoist the per-source coverage computation out to the caller when this area is next touched |
| 2026-09-11 | **Proving a negative needed a poison fixture, not a well-formed one** | T3.5 had to assert that `--events` never consults the locations config. A valid config at the default path would not have proved it: a reader that parses the file and discards the result passes that test. The fixture is therefore a deliberately malformed TOML that `sources.load_sources` raises on — so if the config were ever consulted the CLI would exit non-zero with a traceback, and non-consultation becomes provable rather than plausible. The string is byte-for-byte the one `test_malformed_toml_names_the_line` already proves raises, so the fixture's poisonousness is inherited from a passing test rather than asserted in a comment. The companion `--data-dir` test uses the same shape: a real decoy record placed exactly where an honoured override would resolve, asserted in both directions — the config's record present, the decoy absent |
| 2026-09-11 | **Phase 3 closed: ten defects, and nine of them were the same defect** | Every one of the nine was a value computed correctly, tested thoroughly, and read by nothing: `entry_is_ours` tolerance, "only what THIS call emptied", index preservation on replace, `--settings` never defaulting, `detect.sh`'s migration promise, the `foreign-plus-ours-*` fixture names, ruling (a)'s per-home sub-lines, the per-home `newest_ts` not borrowing, and the firing union wired to a single source. Six surfaced in phase 2, three here. **A mutation found all nine; reading the diff found none.** The reason is structural rather than cultural: test coverage answers "is this value correct?" and almost never answers "does anything read it?" — so a thorough suite around a value that nothing consumes is green by construction. The tenth defect had a different cause and needed a different instrument: a test helper comparing filesystem free space instead of file mtimes, invisible on macOS, invisible on an idle Linux box, and surfaced only once a draft PR put the branch on a loaded CI runner for the first time |
| 2026-09-11 | **What the phase leaves behind, beyond the reader** | Nine CLI-level tests now guard `main()`'s mode branch where there were effectively none — each one added because a mutation showed the wiring, not the function, was unprotected; inverting that branch now kills all nine. A golden fixture frozen at `eb9b529` that turns "did this change `--events`?" from a judgement call into a byte comparison run on two interpreters in CI, with `--write` refusing to overwrite it without `--force`. And a draft PR, because `tests.yml` triggers only on pull requests and pushes to `main`, so seven commits had reached origin with nothing checking them — including phase 2's, which had never run on Linux at all. Also validated in passing: `walk_skill_agent_inventory`'s unreachable detection, checked against a case whose answer was independently known for the first time, correctly identifying 15 shipped skills that can never fire |
| 2026-09-11 | Phase 4 ruling (r): the command ships a verb dispatcher at `lib/setup.sh`, `SKILL.md` wraps it | The SDD Directory Map lists only `SKILL.md` plus `lib/`, but T4.1 step 4 requires the command be exercised through its real entry point rather than its libraries, and bats cannot exercise Markdown. A dispatcher is the only shape under which both hold. `git-setup` is no counter-precedent -- it has no entry-point test either, which is the gap this closes |
| 2026-09-11 | Phase 4 ruling (s): T4.1 implements the legacy migration, T4.2 runs it here | `detect.sh:365` promises the user that setup migrates the legacy shape, and nothing performs it: `entry_is_ours()` gates removal on the `$HOME` namespace, so `--remove` steps over commands pointing at `$CLAUDE_PROJECT_DIR`. The migration is cross-file (`settings.json` out, `settings.local.json` in) while `registration.py` takes one `--settings`. Building and running it are separated so the repository the collection depends on is changed under per-target confirmation |
| 2026-09-11 | Phase 4 ruling (t): the write-path ignore check moves into `setup.sh`, above the lock | SDD-AC-6 was enforced by nothing at command level -- `detect.sh` gates only the settings path while `written_paths()` declares four, and T4.2 left the backup path to an operator's memory. It sits above the lock because the lock file is itself one of the declared paths. Beyond T4.1's literal scope, kept deliberately: the SDD calls this the one refusal that protects a third party |
| 2026-09-11 | Phase 4 ruling (u): `remove` takes legacy entries out too, via `--remove-legacy` | Ruling (s) gave the migration to `install` alone, so `remove` against an un-migrated LEGACY target reported success while all three legacy hooks kept firing -- off to whoever asked, on in fact. PRD F2's premise is that removal makes recording a reversible decision, which a false success defeats |
| 2026-09-11 | Phase 4 ruling (v): PRD F1's foreign-entry preservation is prose-only at command level | `detect.sh:336` collects every command outside our namespace, not only those under our three event names, so CONFLICT is broader than the SDD's wording and install never runs against a target holding foreign content. The property is pinned at the library layer by T2.x; at the command layer nothing exercises it. T4.4's evidence map names it rather than counting it, as spec-018's map did for its own four |
| 2026-09-11 | Phase 4 ruling (w): the registration-matrix suite gains cases, it does not lose its flagless assertion | Ruling (s) said a green run there is the defect, readable as expecting the assertion to disappear. It stays: it pins `registration.py` editing only the one `--settings` path it is handed, correct and deliberate under ADR-1. Only its stale forward-reference to T4.1 was removed |
| 2026-09-11 | `git-setup/SKILL.md`'s `$CLAUDE_PLUGIN_ROOT` interpolation filed as issue #163, not fixed here | Measured empty in a Bash-tool subprocess, so all six command paths in that file expand to a root-relative path that does not exist. It is another plugin's file and outside spec 019; its bats suite is green because it exercises `lib/*.sh` directly and never asserts that the paths SKILL.md prints resolve |
| 2026-09-11 | Phase 4 ruling (x): one matching legacy entry is enough to classify LEGACY | All-three-or-nothing meant a partial shape read as CLEAN, so `install` added a full registration beside still-live legacy hooks and the target recorded twice while `remove` reported success. Four distinct shapes reached it, measured. Fails in the dangerous direction; one-or-more converges a half-migrated target to migrated. `ABORT` rejected -- phase 2 made LEGACY a WARN deliberately. Zero entries still classifies CLEAN |
| 2026-09-11 | Phase 4 ruling (y): legacy ownership is the namespace; the env flag is corroboration, not a gate | Matching was `command.endswith(<script name>)`, which any third party's script of that name satisfied. Reproduced: two genuine legacy entries plus an unrelated `/opt/other-tool/log_skill.sh` classified LEGACY, and the migration would have deleted a hook this project does not own. A coincidence at the all-three threshold, a licence at ruling (x)'s threshold of one -- so the tightening is (x)'s precondition, not scope creep. ADR-5's namespace rule applied to the legacy shape, which it never had been |
| 2026-09-12 | Collection period ends 2026-09-30 | The PRD's one carried open question, answered. Recording began 2026-09-11 with the migration of this repository; three sources are configured. Without a date, "evaluate later" is the failure mode this spec exists to correct, and the mode spec-018 fell into once already |
| 2026-09-12 | Phase 4 ruling (z): a lock that cannot be created is not a lock that is held | Found by T4.2's first real rollout, not by any test. `_lock_acquire` discarded the creation attempt's stderr, so a write-restricted directory reported "another observability setup run holds the lock" with no such run and no lock file. Fixed by capturing that stderr rather than adding a writability pre-check, which would open a TOCTOU gap and cost the happy path. Verified against the field scenario, not only the fixture |
| 2026-09-12 | T4.2 rollout: three sources configured, one refused on its own merits | This repository migrated off its legacy registration; two targets registered clean. A fourth stopped at CONFLICT (exit 0, nothing written) because foreign hooks occupy the event names -- SDD-AC-4 exercised for real. Ruling (t) separately refused that same target first, because its version control ignored the settings file by name and would have left the backup committable. Every target verified before and after: nothing tracked changed, no pre-existing entry lost |
| 2026-09-12 | Phase 4 ruling (aa): the CONFLICT test honours the event name; (v) superseded | `detect.sh:403` discarded the event when collecting foreign commands, so any foreign hook under any event refused the target -- while SDD-AC-4 and PRD F1 both say "under one of the three event names". A real target whose only hooks were PostToolUse and SessionStart was refused although nothing overlapped, and the stop message asserted an occupation that had not happened. The settings schema is an array of hook groups per event and `add_registration` already appends without replacing, so coexistence is the designed shape. (v) diagnosed this and filed it as an evidence-map matter, which was too weak |
| 2026-09-12 | T4.3 closed: the three questions are answerable without inference, verified by me rather than by its author | Cost, record location and how to turn it off each have a passage that answers them outright. The author also found the wrong-file instruction at three further sites beyond the one I named, and documented the `homes`-replaces-default trap that caught the rollout itself. Checked two behaviour claims the text makes against the code: matcher-independence matches what `detect.sh` does, and `"~"` in `homes` really is expanded (`sources.py:231`) |
| 2026-09-12 | Phase 4 ruling (ab): an explicitly empty `homes = []` is rejected at config load | `sources.py:351` does `homes or [default_home]`, so an empty list behaves exactly like an omitted key and no test covers the case. Someone writing it most plausibly means "none" and silently gets "the default one". Rejecting matches this spec's established posture for ambiguous config -- duplicate labels and duplicate `repo_root` basenames are both refused at load rather than allowed to corrupt the split later |
| 2026-09-12 | Phase 4 ruling (ac): AC-3, AC-9 and AC-11 get command-level tests; AC-6's four declared paths each get a refusal test | The evidence map found 24 of 26 criteria cleanly tested, 1 PARTIAL, 0 prose-only. The three were tested only against `registration.py` while worded "when setup runs"; ruling (r) established the command is exercised through its real entry point. AC-6 is the criterion the SDD calls the one refusal that protects a third party, and only 2 of the 4 paths `written_paths()` declares had a refusal test |
| 2026-09-12 | SDD-AC-4 is recorded as exactly satisfied, not as a compromise | I left the matcher question open for evidence -- whether a foreign PreToolUse hook at matcher Bash collides with ours at Skill. Spec compliance answered that the criterion never mentions the matcher, so event-level is not the coarser of two readings needing justification: it is the only one the text supports, and a matcher-level narrowing would be an implementer-added restriction. My question was the wrong one; the answer corrects it rather than settling it |
| 2026-09-12 | Cross-file constant drift is now a test rather than a reading task | Sweeping the one reported one-way cross-reference found four constant pairs spanning the shell/Python boundary, plus a fifth invariant nobody had named (the legacy events and the registered events must be the same three). `OUR_NAMESPACE`/`NAMESPACE` -- the pair every ownership decision rests on under ADR-5 -- had no cross-reference at all. Derivation was rejected deliberately: `detect.sh` is the read-only classifier, and importing the editor would let a damaged editor stop a target being classified at all |
| 2026-09-12 | Implementation complete | All four phases shipped on `feat/019-observability-rollout`, PR #162. Four repositories recording, each confirmed by a selfcheck round-trip, collection period 2026-09-11 to 2026-09-30. Evidence map: 26 acceptance criteria, 26 tested, 0 prose-only. CI green on Linux and macOS -- the macOS bats leg is CON-1's only enforcement and it ran. Suites grew from 1120 to 1164 bats and 780 to 806 pytest across phases 3 and 4 |
| 2026-09-12 | Finalisation withdrawn -- the rollout was incomplete and the report could not see it | Measured after closing: no container home holds the bundle, so the registration's `$HOME/...` command expands to nothing there, the hook fails, and Claude Code ignores it fail-open. A container session in one target ran today and recorded nothing. Configured, healthy-looking, silent -- the signature this spec exists to remove. T4.2 reopens under ruling (ad) |


## Context

### What spec-018 delivered, and what it did not

spec-018 built and proved the recorder: instruction loads with reason and parent, byte cost split
between the always-loaded and conditional layers, skill and agent firings joined against the shipped
inventory, structurally unreachable skills, and per-hook durations under a temporarily installed
wrapper. Every acceptance criterion carries evidence.

What it did not deliver is data. The record has run in one repository for one session. Two figures
from that session are sound at any sample size — the always-loaded layer costs 7038 bytes, and 15
skills in one plugin are structurally unreachable, nested too deep to be discoverable at all. One is
not a finding: "82 of 83 never fired" over a single session describes the sample, not the inventory.

### The four target repositories, verified 2026-09-08

Named `repo1`–`repo4` deliberately; see the Decisions Log. What matters here is not which they are
but that they fall into **two kinds**, because the two kinds put the record in different places.

| Target | Runs in | Record location shape |
|---|---|---|
| repo1 | container | `<repo>/claude-docker-home/.claude/plugins/data/observability-<repo>/observability/events.jsonl` |
| repo2 | **host** | `$HOME/.claude/plugins/data/observability-<repo>/observability/events.jsonl` |
| repo3 | container | container shape, as repo1 |
| repo4 | container | container shape, as repo1 |

Three of the four run Claude in a per-repository container whose home is bind-mounted from
`<repo>/claude-docker-home`; the fourth runs on the host. (Where a target repository contains Docker
configuration of its own, that belongs to the application it ships and has nothing to do with how
Claude runs there — the two were easy to confuse and are not the same thing.)

All four are git repositories. **Corrected 2026-09-08 after a consistency audit:** an earlier
version of this paragraph concluded from "all four already have a `.claude/settings.json`" that
setup only ever extends an existing file. That conclusion was drawn about the wrong file. Setup
writes to `.claude/settings.local.json` (see the Decisions Log), and while that file happens to
exist in all four targets today, nothing guarantees it in general — so **setup must handle an absent
file by creating it**, which is a tested criterion (SDD-AC-2) and a fixture in the phase-2 matrix.
An implementer trusting the old wording would have dropped that path. No record exists in any of the
four yet.

### The three constraints this spec has to satisfy

1. **Two path shapes, not one.** A container repository's record lands inside the repository
   directory under `claude-docker-home/`; a host repository's lands under the user's real `$HOME`.
   Any location config must express both. Note that `claude-docker-home/` is gitignored, so
   spec-018's SDD-AC-10 *intent* (the record cannot be committed) survives even though its literal
   wording, "outside the repository working tree", does not hold inside a container.
2. **`report.py` reads exactly one record.** Its `--events` flag takes a single path. Reading N
   records as one logical set is new work — though the same shape as the rotated-chain merge it
   already performs.
3. **Aggregating at report time is not the same as pooling at rest.** spec-018 deliberately rejected
   a single cross-repository record because it would "mix client contexts into one file". Keeping
   the records separate and merging them for a report does not violate that decision — but the
   report's *output* would still place four contexts side by side, and that needs a deliberate
   answer rather than an accident. Skill and agent usage aggregates harmlessly; file paths from four
   contexts are a different question, and so are the repository names themselves.

### Enabling must never become automatic

spec-018's PRD Feature 3 names the persona as an "ordinary Claude Code user; may not know the
plugins ship hooks at all", whose pain point is that "a plugin update that silently starts recording
their Bash command lines would be a breach of trust, and they would have no reason to look for it".
A setup command is invoked by a person. It is not a prompt at session start, and it is not something
a plugin update performs.

Note also the mechanical limit, which settles the question independently of whether it were
desirable: a hook is a child process and cannot set an environment variable in the session that
spawned it. "Enable itself on demand" is not available in that form at all. The most a hook could do
is write into a configuration file, which takes effect for the *next* session — a different act, and
one that needs a person behind it.

---
*This file is managed by the xdd-meta skill.*
