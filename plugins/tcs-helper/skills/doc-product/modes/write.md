# `write` Mode — Section-by-Section Page Drafting

> **Status: TODO — implemented in Phase 4** of spec 010-doc-product-skill.
> See `docs/XDD/specs/010-doc-product-skill/plan/phase-4.md` (T4.3, T4.4).
>
> This stub exists so the mode router (`SKILL.md`) can dispatch successfully.
> When invoked today, this mode prints the message below and exits.

---

`write` mode is not yet implemented. Phase 4 of the implementation plan delivers:

- Section-structure proposal per page type (driven by `reference/conventions.md`)
- Author confirmation before drafting
- Iterative section-by-section refinement
- Preservation of prior approved sections during iteration
- "Can anything be removed?" prompt after 3 unchanged iterations

To proceed today, use one of the implemented modes (when their phase lands):
- Phase 2 → `review` mode (reader-test killer feature)
- Phase 3 → `extract` mode (configuration reference from settings source)
- Phase 4 → `plan` and `write` modes
